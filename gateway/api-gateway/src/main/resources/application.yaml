server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway.server.webflux:
      routes:
        - id: security-service
          uri: http://localhost:8082
          predicates:
            - Path=/auth/**

#/todos/1
#http://localhost:8080/auth/todos/1

jwt:
  public-key: ${JWT_PUBLIC_KEY}